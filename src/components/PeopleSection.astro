---
import { team } from '../data/team';
const leaders = team.filter(member => member.group === 'leadership');
const students = team.filter(member => member.group !== 'leadership');
const teamStats = [{value: team.length, label: 'Members', labelZh: ''}, {value: team.filter(m => m.role === 'PhD candidate').length, label: 'PhD candidates', labelZh: ''}, {value: team.filter(m => m.role === 'MSc candidate').length, label: 'MSc candidates', labelZh: ''}];
---

<section class="people-header-section" aria-labelledby="people-title">
  <div class="people-header-inner">
    <header class="people-header">
      <h2
        id="people-title"
      >The people doing the work</h2>
      <a class="people-link" href="/our-team/">
        <span class="people-link-label">View Team</span>
        <svg class="people-link-icon" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <circle class="button-primary-circle" cx="16" cy="16" r="16"></circle>
          <path d="M8 16.956h12.604l-3.844 4.106 1.252 1.338L24 16l-5.988-6.4-1.252 1.338 3.844 4.106H8v1.912z" class="color-spacesuit-white"></path>
        </svg>
      </a>
    </header>
    <p
      class="people-subtitle"
    >A research community in Cape Town, connecting ideas across networks, intelligence and engineering.</p>
  </div>
</section>

<section class="people-hero" id="people" aria-label="Intelligent Connectivity Group team">
  <img
    class="people-hero-background"
    src="/assets/people-bg.jpg"
    alt="Earth-observation satellite above the planet"
    fetchpriority="low"
    decoding="async"
  />

  <div class="people-hero-inner">
    <div class="people-panels">
      <article class="people-panel people-pi-panel">
        <p class="people-lead-label">GROUP LEADERSHIP</p>
        {leaders.map(member => (
          <a class="people-pi-heading people-lead" href={`/our-team/${member.slug}/`}>
            <img class="people-pi-photo" src={member.img} alt="" loading="lazy" />
            <div><h3>{member.name}</h3><p class="people-pi-role">{member.role}</p><p class="people-pi-affiliation">Electrical Engineering · UCT</p></div>
          </a>
        ))}
        <p class="people-pi-bio">Bringing together researchers in intelligent connectivity at the University of Cape Town.</p>
        <div class="people-focus" aria-label="Research focus"><span>Software-based networks</span><span>Machine learning</span><span>Network security</span></div>
      </article>

      <article class="people-panel people-team-panel">
        <header class="people-team-heading">
          <div>
            <h3>Our research community</h3>
          </div>
          <a class="people-panel-link" href="/our-team/">
            <span>View all members</span>
            <span aria-hidden="true">→</span>
          </a>
        </header>

        <div class="people-stats" aria-label="Team composition">
          {teamStats.map((stat) => (
            <div class="people-stat">
              <strong>{stat.value}</strong>
              <span data-en={stat.label} data-zh={stat.labelZh}>{stat.label}</span>
            </div>
          ))}
        </div>

        <div class="people-carousel-shell" data-people-carousel>
          <button class="people-carousel-button" type="button" data-carousel-previous aria-label="Previous members" title="Previous members">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m15 18-6-6 6-6" /></svg>
          </button>
          <div class="people-member-carousel" data-carousel-track tabindex="0">
            {students.map((member) => (
              <a class="people-member" href={`/our-team/${member.slug}/`}>
                <img src={member.img} alt="" loading="lazy" decoding="async" />
                <span class="people-member-name">{member.name}</span>
                <span class="people-member-role" data-en={member.role} data-zh={member.roleZh}>{member.role}</span>
              </a>
            ))}
          </div>
          <button class="people-carousel-button" type="button" data-carousel-next aria-label="Next members" title="Next members">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m9 18 6-6-6-6" /></svg>
          </button>
        </div>

        <p
          class="people-team-copy"
        >Our group brings together academic staff, MSc candidates and PhD candidates in the Department of Electrical Engineering at UCT.</p>

        <div class="people-strengths">
          <span>Intelligent networks</span>
          <span>Secure systems</span>
          <span>Connected technologies</span>
        </div>
      </article>
    </div>
  </div>
</section>

<script>
  const initializePeopleCarousels = () => {
    document.querySelectorAll<HTMLElement>('[data-people-carousel]').forEach((carousel) => {
      if (carousel.dataset.ready === 'true') return;

      const track = carousel.querySelector<HTMLElement>('[data-carousel-track]');
      const previous = carousel.querySelector<HTMLButtonElement>('[data-carousel-previous]');
      const next = carousel.querySelector<HTMLButtonElement>('[data-carousel-next]');
      if (!track || !previous || !next) return;

      carousel.dataset.ready = 'true';

      const updateButtons = () => {
        const maximum = track.scrollWidth - track.clientWidth;
        previous.disabled = track.scrollLeft <= 2;
        next.disabled = track.scrollLeft >= maximum - 2;
      };

      const scroll = (direction: number) => {
        track.scrollBy({
          left: direction * Math.max(track.clientWidth * 0.72, 180),
          behavior: 'smooth',
        });
      };

      previous.addEventListener('click', () => scroll(-1));
      next.addEventListener('click', () => scroll(1));
      track.addEventListener('scroll', updateButtons, { passive: true });
      new ResizeObserver(updateButtons).observe(track);
      updateButtons();
    });
  };

  initializePeopleCarousels();
  document.addEventListener('astro:page-load', initializePeopleCarousels);
</script>

<style>
  .people-lead-label { margin: 0 0 24px; font-size: 11px; letter-spacing: .14em; color: #a5dcea; font-weight: 700; }
  .people-lead { color: white; margin-bottom: 24px; }
  .people-lead h3 { font-size: 25px; }
  .people-lead .people-pi-photo { width: 78px; height: 78px; }
  .people-lead.people-pi-heading { grid-template-columns: 78px minmax(0, 1fr); }

  .people-header-section {
    position: relative;
    z-index: 2;
    padding: var(--home-section-gap) 0 48px;
    color: #111;
    background: #fff;
  }

  .people-header-inner {
    width: min(calc(100% - 96px), 1260px);
    margin-inline: auto;
  }

  .people-header {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: start;
    gap: 64px;
  }

  .people-header h2 {
    margin: 0;
    color: #111;
    font-family: "Sora", sans-serif;
    font-size: 48px;
    font-weight: 800;
    line-height: 1.05;
    letter-spacing: 0;
    white-space: nowrap;
  }

  .people-link {
    position: relative;
    z-index: 1;
    display: inline-grid;
    grid-template-columns: max-content 28px;
    align-items: center;
    gap: 12px;
    margin-top: 10px;
    color: #111;
    font-family: "Sora", sans-serif;
    font-size: 17px;
    font-weight: 760;
    white-space: nowrap;
    text-decoration: none;
  }

  .people-link-icon {
    position: relative;
    z-index: 2;
    display: block;
    width: 28px;
    height: 28px;
    overflow: visible;
    transition: transform 180ms ease;
  }

  .people-link-icon .button-primary-circle {
    fill: #007fa3;
  }

  .people-link-icon .color-spacesuit-white {
    fill: #fff;
  }

  .people-link:hover .people-link-icon {
    transform: translateX(3px);
  }

  .people-subtitle {
    max-width: 620px;
    margin: 24px 0 0;
    color: #555;
    font-size: 18px;
    line-height: 1.55;
  }

  .people-hero {
    position: relative;
    isolation: isolate;
    width: 100%;
    height: 88vh;
    height: 88svh;
    min-height: 760px;
    max-height: 940px;
    overflow: hidden;
    color: #fff;
    background: #02050a;
  }

  .people-hero::after {
    position: absolute;
    inset: 0;
    z-index: 1;
    content: "";
    pointer-events: none;
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.02) 32%, rgba(0, 6, 15, 0.46) 100%);
  }

  .people-hero-background {
    position: absolute;
    inset: 0;
    z-index: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .people-hero-inner {
    position: absolute;
    inset: 0;
    z-index: 2;
    display: flex;
    align-items: center;
    padding: 48px 0;
  }

  .people-panels {
    display: grid;
    grid-template-columns: minmax(0, 0.88fr) minmax(0, 1.12fr);
    align-items: stretch;
    gap: 24px;
    width: min(calc(100% - 96px), 1320px);
    margin-inline: auto;
  }

  .people-panel {
    min-width: 0;
    min-height: 580px;
    padding: 34px 32px;
    border: 1px solid rgba(255, 255, 255, 0.13);
    border-radius: 8px;
    background: rgba(5, 14, 26, 0.34);
    backdrop-filter: blur(18px) saturate(0.82);
    -webkit-backdrop-filter: blur(18px) saturate(0.82);
  }

  .people-pi-panel {
    display: grid;
    grid-template-rows: repeat(5, auto);
    align-content: space-between;
    row-gap: 18px;
  }

  .people-pi-heading {
    display: grid;
    grid-template-columns: 108px minmax(0, 1fr);
    align-items: center;
    gap: 22px;
  }

  .people-pi-photo {
    width: 108px;
    height: 108px;
    border: 1px solid rgba(255, 255, 255, 0.42);
    border-radius: 50%;
    object-fit: cover;
  }

  .people-pi-heading h3,
  .people-team-heading h3 {
    margin: 0;
    color: #fff;
    font-family: "Sora", sans-serif;
    font-weight: 760;
    letter-spacing: 0;
  }

  .people-pi-heading h3 {
    font-size: 28px;
    line-height: 1.12;
  }

  .people-pi-heading h3 span {
    display: block;
    margin-top: 4px;
    color: rgba(255, 255, 255, 0.62);
    font-size: 16px;
    font-weight: 560;
  }

  .people-pi-role {
    margin: 10px 0 0;
    color: rgba(255, 255, 255, 0.72);
    font-size: 14px;
    line-height: 1.4;
  }

  .people-pi-affiliation {
    max-width: 330px;
    margin: 5px 0 0;
    color: rgba(255, 255, 255, 0.56);
    font-size: 12px;
    line-height: 1.4;
  }

  .people-pi-bio {
    margin: 0;
    color: rgba(255, 255, 255, 0.82);
    font-size: 16px;
    line-height: 1.62;
  }

  .people-focus {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 0;
    margin-top: 0;
    color: rgba(255, 255, 255, 0.7);
    font-size: 13px;
    font-weight: 650;
  }

  .people-focus span {
    display: inline-flex;
    align-items: center;
  }

  .people-focus span:not(:last-child)::after {
    width: 1px;
    height: 13px;
    margin: 0 12px;
    content: "";
    background: rgba(255, 255, 255, 0.28);
  }

  .people-pi-footer {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 20px;
    margin-top: 0;
    padding-top: 0;
  }

  .people-pi-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 0;
  }

  .people-pi-stat {
    min-width: 0;
    min-height: 96px;
    padding: 18px 13px;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.045);
  }

  .people-pi-stat strong,
  .people-pi-stat span {
    display: block;
  }

  .people-pi-stat strong {
    color: #fff;
    font-family: "Sora", sans-serif;
    font-size: 21px;
    line-height: 1.1;
    white-space: nowrap;
  }

  .people-pi-stat span {
    margin-top: 8px;
    color: rgba(255, 255, 255, 0.56);
    font-size: 11px;
    line-height: 1.3;
  }

  .people-pi-links {
    display: flex;
    flex-wrap: wrap;
    gap: 10px 15px;
  }

  .people-pi-links a,
  .people-panel-link {
    color: rgba(255, 255, 255, 0.78);
    font-size: 14px;
    font-weight: 650;
    text-decoration: none;
    transition: color 180ms ease;
  }

  .people-pi-links a:hover,
  .people-panel-link:hover {
    color: #fff;
  }

  .people-pi-links a {
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }

  .people-pi-links img,
  .people-pi-links svg {
    width: 18px;
    height: 18px;
    flex: 0 0 auto;
  }

  .people-pi-links img {
    filter: grayscale(1) brightness(2.2);
    opacity: 0.82;
  }

  .people-pi-links svg {
    fill: currentColor;
  }

  .people-panel-link {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 8px;
  }

  .people-team-panel {
    display: grid;
    grid-template-rows: repeat(5, auto);
    align-content: space-between;
    row-gap: 18px;
  }

  .people-team-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 24px;
  }

  .people-team-heading h3 {
    font-size: 24px;
    line-height: 1.2;
  }

  .people-stats {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 0;
  }

  .people-stat {
    min-width: 0;
    min-height: 88px;
    padding: 16px 14px;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.045);
  }

  .people-stat strong,
  .people-stat span {
    display: block;
  }

  .people-stat strong {
    color: #fff;
    font-family: "Sora", sans-serif;
    font-size: 27px;
    line-height: 1;
  }

  .people-stat span {
    margin-top: 8px;
    overflow-wrap: anywhere;
    color: rgba(255, 255, 255, 0.58);
    font-size: 12px;
    line-height: 1.3;
  }

  .people-carousel-shell {
    display: grid;
    grid-template-columns: 34px minmax(0, 1fr) 34px;
    align-items: center;
    gap: 10px;
    margin-top: 0;
    padding-top: 10px;
  }

  .people-carousel-button {
    display: grid;
    width: 34px;
    height: 34px;
    padding: 0;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    place-items: center;
    color: rgba(255, 255, 255, 0.82);
    background: rgba(255, 255, 255, 0.06);
    cursor: pointer;
    transition: color 180ms ease, background 180ms ease, opacity 180ms ease;
  }

  .people-carousel-button:hover:not(:disabled) {
    color: #fff;
    background: rgba(255, 255, 255, 0.14);
  }

  .people-carousel-button:disabled {
    cursor: default;
    opacity: 0.28;
  }

  .people-carousel-button svg {
    width: 17px;
    height: 17px;
    fill: none;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .people-member-carousel {
    display: grid;
    grid-auto-columns: calc((100% - 56px) / 5);
    grid-auto-flow: column;
    gap: 14px;
    min-width: 0;
    overflow-x: auto;
    overscroll-behavior-inline: contain;
    scrollbar-width: none;
    scroll-snap-type: x mandatory;
  }

  .people-member-carousel::-webkit-scrollbar {
    display: none;
  }

  .people-member {
    display: grid;
    min-width: 0;
    justify-items: center;
    color: #fff;
    text-align: center;
    text-decoration: none;
    scroll-snap-align: start;
  }

  .people-member img {
    width: 58px;
    max-width: 100%;
    height: auto;
    aspect-ratio: 1;
    border: 1px solid rgba(255, 255, 255, 0.38);
    border-radius: 50%;
    object-fit: cover;
    transition: border-color 180ms ease, transform 180ms ease;
  }

  .people-member:hover img {
    border-color: rgba(255, 255, 255, 0.82);
    transform: translateY(-2px);
  }

  .people-member-name,
  .people-member-role {
    width: 100%;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .people-member-name {
    margin-top: 9px;
    font-size: 13px;
    font-weight: 700;
  }

  .people-member-role {
    margin-top: 3px;
    color: rgba(255, 255, 255, 0.5);
    font-size: 11px;
  }

  .people-team-copy {
    margin: 0;
    padding: 19px 20px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 6px;
    color: rgba(255, 255, 255, 0.68);
    background: rgba(255, 255, 255, 0.035);
    font-size: 15px;
    line-height: 1.6;
  }

  .people-strengths {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 0;
  }

  .people-strengths span {
    display: flex;
    min-height: 72px;
    align-items: center;
    padding: 12px 14px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 6px;
    color: rgba(255, 255, 255, 0.76);
    background: rgba(255, 255, 255, 0.035);
    font-size: 14px;
    font-weight: 650;
    line-height: 1.35;
  }

  @media (max-width: 1120px) {
    .people-header-inner,
    .people-panels {
      width: min(calc(100% - 48px), 940px);
    }

    .people-header h2 {
      font-size: 40px;
      white-space: normal;
    }

    .people-hero {
      height: auto;
      min-height: 1120px;
      max-height: none;
    }

    .people-hero-inner {
      position: relative;
      padding: 80px 0;
    }

    .people-panels {
      grid-template-columns: 1fr;
    }

    .people-panel,
    .people-pi-panel,
    .people-team-panel {
      min-height: 0;
    }

  }

  @media (max-width: 700px) {
    .people-header-section {
      padding: var(--home-section-gap) 0 36px;
    }

    .people-header-inner,
    .people-panels {
      width: calc(100% - 40px);
    }

    .people-header {
      grid-template-columns: 1fr;
      gap: 18px;
    }

    .people-header h2 {
      font-size: 34px;
    }

    .people-link {
      margin-top: 0;
      font-size: 16px;
    }

    .people-subtitle {
      margin-top: 20px;
      font-size: 16px;
    }

    .people-hero {
      min-height: 0;
    }

    .people-hero-inner {
      padding: 60px 0 36px;
    }

    .people-panel {
      padding: 24px 22px;
    }

    .people-pi-heading {
      grid-template-columns: 82px minmax(0, 1fr);
      gap: 16px;
    }

    .people-pi-photo {
      width: 82px;
      height: 82px;
    }

    .people-pi-heading h3 {
      font-size: 23px;
    }

    .people-pi-bio {
      margin-top: 22px;
      font-size: 15px;
    }

    .people-pi-footer,
    .people-team-heading {
      align-items: flex-start;
      flex-direction: column;
    }

    .people-team-heading {
      display: flex;
      gap: 12px;
    }

    .people-stats {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .people-member-carousel {
      grid-auto-columns: calc((100% - 24px) / 3);
      gap: 12px;
    }

    .people-strengths {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 420px) {
    .people-member-carousel { grid-auto-columns: calc((100% - 12px) / 2); }
    .people-pi-stats {
      grid-template-columns: 1fr;
    }

    .people-pi-stat {
      min-height: 0;
    }
  }
</style>
